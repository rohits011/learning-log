# Pending Tasks

- [ ] **AI Q&A Agent Integration**: Implement an embedded AI agent feature in the UI. This agent will automatically ingest the markdown files in `public/docs/` and generate interactive Q&A (flashcards) directly in the UI to test knowledge on the documents.
